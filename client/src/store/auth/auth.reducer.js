const initialState = {}

export default function authGymPad(state = initialState, action) {
  switch (action.type) {
    case 'ACTION':
      return { ...state }
    default:
      return { ...state }
  }
}